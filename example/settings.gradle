plugins {
    id("com.gradle.enterprise").version("3.4.1")
}

rootProject.name = "01-local-cache"

gradleEnterprise {
    buildScan {
        server = "https://enterprise-training.gradle.com"
        publishAlways()
        captureTaskInputFiles = true
        tag("lab01")
    }
}
